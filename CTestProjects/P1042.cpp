#include <bits/stdc++.h>
using namespace std;
int main() {
	string s;
	char c = ' ';
	while (c != 'E') {
		cin >> c;
		if (c != 'E') s += c;
	}
	int l, e, r = round(s.size() / 11.0);
	for (int i = 1; i <= r; i++) {
		for(int n = 1;)
	}
	return 0;
}
